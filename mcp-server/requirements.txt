# Pinned, matching backend/ and rules-service/. These were `>=` ranges, which
# meant CI silently installed mcp 2.0.0 the day it was published — a major
# release that moved `mcp.server.fastmcp`, so the import in server.py stopped
# resolving. Nothing in this repo changed; the world did.
#
# Upgrading to mcp 2.x is a real task (the FastMCP entry point moved), not a
# version bump. Pinned to the 1.x line the code targets until that is done.
#
# httpx is pinned to satisfy mcp's own floor (httpx>=0.27.1,<1.0.0). Do not
# copy rules-service's 0.27.0 here — it is one patch below that floor, which
# makes the resolution impossible rather than merely wrong.
mcp==1.28.1
httpx==0.28.1
